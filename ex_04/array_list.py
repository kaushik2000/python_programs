number_list = list()
size = int( input('Enter the size of number-list: ') )
count = 0

for count in range
    number_list[count] = input('Enter the number no.', count + 1)
    print(count, number_list[count])